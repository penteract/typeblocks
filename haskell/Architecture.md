
The Gloss API is a decent one to build to, particularly if I've got an eye to compiling to js at some point.

The missing things are something to tell me the length of a piece of text, and a good way to efficiently represent animations (most of the render tree doesn't need to be recalculated every frame)
